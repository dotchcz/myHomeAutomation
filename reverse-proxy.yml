apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myhomeautomation-ingress
spec:
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: myhomeautomation-webapi
                port:
                  number: 5266